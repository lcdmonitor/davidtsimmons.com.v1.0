using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace DavidSimmons.Tests
{
    [TestClass]
    public class SampleTest
    {
        [TestMethod]
        public void true_should_be_true()
        {
            //var mockedRepository = MockRepository.GenerateMock<ISignalRRepository>();

            //Assert.IsNotNull(mockedRepository);
            Assert.IsTrue(true);
        }
    }
}
